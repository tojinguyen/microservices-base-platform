---
# Redis dùng cho identity-service (lưu token cache, session)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-identity
  namespace: microservices-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-identity
  template:
    metadata:
      labels:
        app: redis-identity
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 6379
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
          readinessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 5
            periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: redis-identity-service
  namespace: microservices-platform
spec:
  type: ClusterIP
  selector:
    app: redis-identity
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
